{
    "jest": {
        "testEnvironment": "node"
    },
    "scripts": {
        "test": "export NODE_ENV=test && jest --forceExit --runInBand"
    },
    "devDependencies": {
        "jest": "^27.5.1",
        "mongodb-memory-server": "^8.4.1"
    },
    "dependencies": {
        "express": "^4.18.0",
        "mongoose": "^6.3.1",
        "supertest": "^6.2.3"
    }
}
